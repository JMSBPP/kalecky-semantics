import Kalecky.Types.Units.Unit

-- todo: This needs syntax for a derivation rule

-- \(c \, (u_s (k), v_h (l)) := \frac{s = h}{\rho (u_s (k), v_h (l)) | \tau (u_s (k), v_h (l))}\)


-- per := \(\rho\)

-- \(\rho (u_s (k), v_h (l)) \equiv \frac{u_h (l)}{v_h (l)}\)


-- times := \(\tau\)

-- \(\tau (u_s (k), v_h (l)) \equiv u_h (l) \otimes v_h (l)\)


