---
plan: 03-01
phase: 3
wave: 1
depends_on: []
autonomous: false
requirements: [ALG-01, ALG-02, ALG-03, ALG-04, ALG-05, ALG-06]
files_modified:
  - kalecky-spec/src/Kalecky/Types/Measure.hs
  - kalecky-spec/src/Kalecky/Operators/**
  - kalecky-spec/test-kalecky/**
  - kalecky-spec/kalecky-spec.cabal
execution_mode: live-session
---

# Plan 03-01: Algebraic Operators — Live Co-Design Ledger

<objective>
Build the economics-agnostic algebra bottom-up as live in-session co-design increments, per 03-CONTEXT.md (Gap redefined as expectation-vs-realized; Delta added for realized differences). Same process as Phase 2: discuss → draft tests → approve → RED → GREEN → tick.
</objective>

<increments>
- [x] **1. Measure & Expectation** ✓ shipped 2026-08-16 (59/59 green) — — type-level `Agent` (Household | Firm | Government | FinancialSector), `Measure` carrying the agent, `Expectation (μ :: Measure) x` (E^μ) [ALG-03]
- [x] **2. Gap** ✓ shipped 2026-08-16 (66/66 green; TwoRealized + MeasureMix boundary files) — expectation-vs-realized, type-enforced; `gapER`/`gapRE` + lawful `flip` (negate law); signed-exact evaluation class instanced by Unit/Price [ALG-01, ALG-02 partial]
- [x] **3. Delta** ✓ shipped 2026-08-16 (71/71 green) — realized-to-realized oriented difference in `Operators/Delta.hs`; same evaluation class; the Tasa→Tasa carrier [ALG-02]
- [ ] **4. GrowthRate & CommonGrowthRate** — exact signed Rational Δx/x, independent primitive, from-observations constructor (Maybe on zero base); `mkCommonGrowthRate` Just on exact rate equality [ALG-05, ALG-06]
- [ ] **5. Effect** — newtype over exact signed Rational (∂responder/∂perturband), phantom responder/perturband [ALG-04]
</increments>

<verification>
- `cd kalecky-spec && cabal test kalecky-test` exits 0 with all increment groups green
- `./scripts/check-compile-fail.sh` still exits 0 (plus any new boundary files this phase adds)
- Every increment has RED and GREEN commits with conventional prefixes
</verification>

<must_haves>
- A Gap of two realized values is unrepresentable; `evalGap (flip g) == negate (evalGap g)` proven
- E^H[x] and E^F[x] are distinct types; Delta (GrowthRate x) types the +20bp case
- No floats: Integer/Rational only at evaluation; every approval covered laws + a concrete example
</must_haves>
