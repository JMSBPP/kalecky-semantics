---
plan: 02-01
phase: 2
wave: 1
depends_on: []
autonomous: false
requirements: [UNIT-01, UNIT-02, UNIT-03, UNIT-04, UNIT-05, UNIT-06, PROOF-01]
files_modified:
  - kalecky-spec/src/Kalecky/Types/**
  - kalecky-spec/test-kalecky/**
  - kalecky-spec/kalecky-spec.cabal
execution_mode: live-session
---

# Plan 02-01: Dimensional Kernel — Live Co-Design Ledger

<objective>
Build the dimensional kernel bottom-up as live in-session co-design increments. This plan is a LEDGER, not an executor script: each increment is discussed, its tests drafted and approved by the user in-conversation, then implemented RED→GREEN and ticked off here. No subagents. (Process decision recorded in PROJECT.md Key Decisions, 2026-08-15.)
</objective>

<process_per_increment>
1. Discuss laws live (resolve any open question from 02-RESEARCH.md for this increment)
2. Draft test code in-session; user approves (laws + at least one concrete example)
3. Commit `test(kalecky): …` (RED) → implement → `cabal test kalecky-test` green → commit `feat(kalecky): …` (GREEN)
4. Tick the increment below; contradictions with an approved test stop the increment and get surfaced
</process_per_increment>

<increments>
- [x] **1. Scale** — per-basis scale functions (`denomination_scale` Raw=1/Thousand=10³/Million=10⁶/Billion=10⁹; `LaborScale` WORKER_BASE=1; `TimeScale` MONTH_BASE=2592000) [UNIT-01 partial] ✓ approved+shipped 2026-08-15 (4 laws, 3 examples, 7/7 green)
- [x] **2. Unit** — amount-carrying `Unit u_s(k)`: Natural qty at a scale, per Draft.plk `Unit(ScaleFn, Basis) { qty }`; semigroup under (·) [UNIT-01, UNIT-02 partial] ✓ approved+shipped 2026-08-15 ((·) = amounts×/scales×; basis-typed; 19/19 green incl. quickcheck-classes semigroup lawsets)
- [x] **3. Base units** — `Currency` (COP tradeable_base=50, USD), `MoneyUnit`, `LaborUnit` (Worker | LaborHour), minimal `TimeUnit` (Month, Hour) — resolve open questions: HOUR_BASE, LaborUnit denomination axis, Worker/LaborHour additive compatibility [UNIT-01] ✓ approved+shipped 2026-08-15 (HOUR_BASE=3600; no labor denomination; Worker/LaborHour distinct types; USD deferred; src tree layout restored per user correction — 30/30 green)
- [x] **4. CompoundUnit** — `per`/`times` (ρ/τ), exact scale alignment toward finer denomination (`s = h` rule) — resolve open question: alignment scope across kinds [UNIT-02, UNIT-05, UNIT-06] ✓ approved+shipped 2026-08-15 (s=h within kind only; per/times structure-preserving; align/add in Unit.hs; cancel exact-or-Nothing — 43/43 green)
- [ ] **5. Valuation** — `Nominal | Real PriceIndex` — resolve open question: PriceIndex minimal shape [UNIT-03 partial]
- [ ] **6. Price** — `Price p(u,v)`: valuation-parameterized Per-compound unit; restricted arithmetic; compile-fail files for COP+USD / money+labor / Nominal+Real [UNIT-03, UNIT-04]
</increments>

<verification>
- `cd kalecky-spec && cabal test kalecky-test` exits 0 with all increment test groups green
- Each compile-fail file under the excluded directory fails `ghc -fno-code` type-checking
- Every increment has a RED and a GREEN commit with conventional prefixes
</verification>

<must_haves>
- Mismatched currency/dimension/valuation arithmetic fails to compile; same-currency denominations auto-align exactly (Natural, multiply-only)
- Every shipped type's laws + concrete example were approved by the user before implementation (PROOF-01)
</must_haves>
