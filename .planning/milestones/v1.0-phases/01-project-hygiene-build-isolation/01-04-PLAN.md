---
phase: 01-project-hygiene-build-isolation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md
autonomous: false
requirements: [INFRA-01]

must_haves:
  truths:
    - "The exact drift between lib/plank-monorepo and upstream is measured and written down, not assumed"
    - "The closest matching upstream commit (or the finding that none exists) is documented with the evidence used to reach it"
    - "The user has chosen how lib/plank-monorepo is tracked, from options grounded in the measured drift"
  artifacts:
    - path: ".planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md"
      provides: "Drift report: differing file list, candidate commits tried, closest match, recommendation, recorded decision"
      min_lines: 40
  key_links:
    - from: "01-plank-monorepo-drift.md"
      to: "Plan 05's conversion task"
      via: "the recorded user decision (option a/b/c/d) drives what Plan 05 executes"
      pattern: "\\*\\*Chosen:\\*\\* option-[abcd]"
---

<objective>
Measure the confirmed drift between `lib/plank-monorepo` and its upstream, write the evidence down, and get an explicit user decision on how it should be tracked. No conversion happens in this plan.

Purpose: 01-RESEARCH.md Pitfall 4 and Open Question 1 established that the user's "all three local copies are unmodified" assumption does NOT hold for `plank-monorepo` — 52 files differed, spanning real source changes across `plankc/frontend/**`, `plankc/sir/**` and `plank-tree-sitter/**`. Silently pinning to upstream HEAD would either destroy local content or produce a submodule whose checkout does not match what is on disk today. The locked decision in 01-CONTEXT.md is explicit: "if a diff shows local modifications, stop and surface it".
Output: a drift report and a recorded user decision.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-hygiene-build-isolation/01-CONTEXT.md
@.planning/phases/01-project-hygiene-build-isolation/01-RESEARCH.md
@.planning/phases/01-project-hygiene-build-isolation/01-03-SUMMARY.md

Verified local state (checked live during planning, 2026-08-15):
- `lib/plank-monorepo` — 1401 files, 25M, NO nested `.git` DIRECTORY
- Top-level entries: `CONTRIBUTING.md`, `LICENSE`, `plankc`, `plank-doc`, `plank-tree-sitter`, `plankup`, `plank-vscode`, `plank-zed`, `README.md`, `std`
- `lib/plank-monorepo/.gitmodules` declares FIVE nested submodules (Pitfall 5):
  `plankc/plank-diff-tests/lib/forge-std`, `plankc/plank-diff-tests/lib/plank-foundry-deployer`, `plankc/plank-diff-tests/lib/solady`, `plankc/sir/sir-solidity-diff-tests/lib/forge-std`, `plankc/sir/sir-solidity-diff-tests/lib/solady`
- Those five nested paths are materialized as directories with real content (e.g. `lib/plank-monorepo/plankc/plank-diff-tests/lib/solady/src/` contains `accounts`, `auth`, `Milady.sol`, `tokens`, `utils`) — the local copy is a flattened vendor snapshot, not a submodule-aware clone
- `remappings.txt` line 4 depends on that path: `solady/=lib/plank-monorepo/plankc/plank-diff-tests/lib/solady/src/`
- Distinguishing files research named: `lib/plank-monorepo/plankc/justfile` (env var renames), `lib/plank-monorepo/plankc/frontend/parser/src/lexer.rs` and/or `lib/plank-monorepo/plankc/sir/crates/parser/src/lexer.rs` (a `Self` token added upstream)

TWO ADDITIONAL FACTS discovered during planning that 01-RESEARCH.md does not mention. The drift report MUST cover both under `## Nested Submodule Impact`:

1. **Five dangling gitdir pointers.** Each nested dependency directory still contains a 71-byte `.git` FILE, e.g.
   `lib/plank-monorepo/plankc/plank-diff-tests/lib/forge-std/.git` -> `gitdir: ../../../../.git/modules/plankc/plank-diff-tests/lib/forge-std`
   That target no longer exists (the parent `.git` was removed), so git currently treats those directories as ordinary directories. Verified: `find lib/plank-monorepo -name .git -type d` returns nothing; `find lib/plank-monorepo -name .git -type f` returns exactly those five paths.

2. **`plank-diff-tests/lib/` is self-ignored upstream.** `lib/plank-monorepo/plankc/plank-diff-tests/.gitignore` line 3 is `lib/`. Verified:
   `git check-ignore -v lib/plank-monorepo/plankc/plank-diff-tests/lib/solady/src/Milady.sol` ->
   `lib/plank-monorepo/plankc/plank-diff-tests/.gitignore:3:lib/`
   Consequence measured with `git add --dry-run lib/plank-monorepo | wc -l` -> **825** of 1401 files would stage; the entire `plankc/plank-diff-tests/lib/**` tree — which hosts the `solady/` remapping target — would be silently omitted. `plankc/sir/sir-solidity-diff-tests/lib/**` is NOT ignored and does stage.

Upstream: `https://github.com/plankevm/plank-monorepo.git` (default-branch HEAD at research time began `d1cbd2c3`; 52 files differed).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the plank-monorepo drift and write the report</name>
  <files>.planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md</files>
  <read_first>
    - .planning/phases/01-project-hygiene-build-isolation/01-RESEARCH.md — Pitfall 4 (the drift finding) and Pitfall 5 (nested submodules), plus "Don't Hand-Roll" row 3 (the bisection procedure: `git log --all -- <path>` / GitHub commits API + raw-content fetch per candidate)
    - lib/plank-monorepo/.gitmodules (the five nested submodule declarations)
    - lib/plank-monorepo/plankc/plank-diff-tests/.gitignore (line 3 is `lib/` — the self-ignore that hides the solady target)
    - remappings.txt (line 4 — `solady/` resolves through a nested submodule path)
    - lib/plank-monorepo/plankc/justfile (one of the two distinguishing files research used)
  </read_first>
  <action>
Produce hard evidence. Do NOT delete or modify `lib/plank-monorepo` in this task.

Step 1 — clone upstream recursively (Pitfall 5: nested submodules are not recursive by default; without `--recurse-submodules` the diff would report false differences for all five nested paths):

```bash
mkdir -p /tmp/kalecky-phase1
rm -rf /tmp/kalecky-phase1/pm.upstream
git clone --quiet --recurse-submodules https://github.com/plankevm/plank-monorepo.git /tmp/kalecky-phase1/pm.upstream
git -C /tmp/kalecky-phase1/pm.upstream rev-parse HEAD
```

Step 2 — full diff against upstream HEAD, captured to a file:

```bash
diff -rq --exclude=.git lib/plank-monorepo /tmp/kalecky-phase1/pm.upstream \
  > /tmp/kalecky-phase1/pm-diff-HEAD.txt 2>&1 || true
wc -l /tmp/kalecky-phase1/pm-diff-HEAD.txt
```

Step 3 — bisect over the history of a distinguishing file. Use `plankc/justfile` (small, changes rarely, research already used it):

```bash
git -C /tmp/kalecky-phase1/pm.upstream log --oneline -40 -- plankc/justfile
```

For each candidate commit (walk newest to oldest, stop early on a clean diff):

```bash
git -C /tmp/kalecky-phase1/pm.upstream checkout --quiet <SHA>
git -C /tmp/kalecky-phase1/pm.upstream submodule update --init --recursive --quiet
diff -rq --exclude=.git lib/plank-monorepo /tmp/kalecky-phase1/pm.upstream | wc -l
```

Record the differing-file count per candidate. Cap the search at 20 candidates or 30 minutes, whichever comes first — an exact match may not exist (research could not find one).

Step 4 — classify the surviving differences. For every path in the final best-candidate diff, determine whether it is:
  (a) present locally but absent upstream (local-only content),
  (b) absent locally but present upstream (upstream-only content),
  (c) present in both with different content.
For up to 5 representative class-(c) files, capture the actual `diff -u` hunk so the user can judge whether the change looks like upstream progress or a local hand-edit.

Step 5 — confirm the two planning-discovered facts still hold, and record the numbers:

```bash
find lib/plank-monorepo -name .git -type f          # expect the 5 dangling gitdir pointers
find lib/plank-monorepo -name .git -type d | wc -l  # expect 0
git add --dry-run lib/plank-monorepo | wc -l        # expect 825 (vs 1401 on disk)
git check-ignore -v lib/plank-monorepo/plankc/plank-diff-tests/lib/solady/src/Milady.sol
```

Step 6 — write `.planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md` with exactly these sections:

```markdown
# lib/plank-monorepo — Upstream Drift Report

**Measured:** <date>
**Upstream:** https://github.com/plankevm/plank-monorepo.git
**Local copy:** lib/plank-monorepo — 1401 files, 25M, no .git directory

## Method
<commands run, recursive clone flags used, bisection cap applied>

## Candidates Tried
| Commit | Date | Subject | Differing files |
|--------|------|---------|-----------------|

## Closest Match
**Commit:** <sha or "none found">
**Differing files:** <n>

## Differing Files (closest match)
### Local-only (present here, absent upstream)
### Upstream-only (absent here, present upstream)
### Content differs
<full list of paths>

## Representative Diffs
<up to 5 `diff -u` hunks>

## Nested Submodule Impact
<must state: the five dangling .git gitfile pointers and their targets; the
 plank-diff-tests/.gitignore `lib/` self-ignore; the measured 825-vs-1401
 `git add --dry-run` count; and whether remappings.txt line 4
 `solady/=lib/plank-monorepo/plankc/plank-diff-tests/lib/solady/src/`
 would still resolve under each of the four options in Task 2>

## Assessment
<is the drift upstream progress, local hand-edits, or both? evidence for the call>

## Recommendation
<which option in Task 2 the evidence favours, and why>
```

Commit with: `docs(phase-1): measure lib/plank-monorepo drift against upstream`
  </action>
  <verify>
    <automated>test -f .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md && test "$(wc -l < .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md)" -ge 40 && grep -q '^## Closest Match' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md && grep -q '^## Nested Submodule Impact' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md && grep -q '^## Recommendation' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md && test "$(find lib/plank-monorepo -type f -not -path '*/.git/*' | wc -l)" -eq 1401 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md` exists and is at least 40 lines
    - It contains all eight headings: `## Method`, `## Candidates Tried`, `## Closest Match`, `## Differing Files (closest match)`, `## Representative Diffs`, `## Nested Submodule Impact`, `## Assessment`, `## Recommendation`
    - The `## Candidates Tried` table has at least 3 data rows, each with a 40-hex commit SHA and an integer differing-file count
    - `## Closest Match` states either a 40-hex SHA or the literal string `none found`
    - `## Nested Submodule Impact` contains the literal path `plankc/plank-diff-tests/lib/solady`, the literal string `825`, and the literal string `gitdir:`
    - `## Representative Diffs` contains at least one fenced code block containing a line starting with `+` or `-`
    - `find lib/plank-monorepo -type f -not -path '*/.git/*' | wc -l` still outputs `1401` (local copy untouched by this task)
    - `git status --porcelain lib/plank-monorepo | grep -v '^??'` produces no output
    - `git log --oneline -1` subject is `docs(phase-1): measure lib/plank-monorepo drift against upstream`
  </acceptance_criteria>
  <done>The drift is measured, classified, and written down with commit-level evidence; the local copy is untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide how lib/plank-monorepo is tracked</name>
  <files>.planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md</files>
  <read_first>
    - .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md (the report just produced — present its Closest Match, Nested Submodule Impact, Assessment and Recommendation sections verbatim to the user)
    - .planning/phases/01-project-hygiene-build-isolation/01-CONTEXT.md — the locked decision this checkpoint exists to honour
  </read_first>
  <action>
Pause and put the decision to the user. Do NOT pick an option autonomously and do NOT touch `lib/plank-monorepo`.

1. Print, verbatim, the drift report's `## Closest Match`, `## Nested Submodule Impact`, `## Assessment` and `## Recommendation` sections.
2. Present the four options below with their pros and cons, stating which one the report's `## Recommendation` favours.
3. Wait for the user to select `option-a`, `option-b`, `option-c` or `option-d`.
4. Append their choice to the report as a new final section, in exactly this shape:

```markdown
## Decision

**Chosen:** option-<x>
**Date:** <YYYY-MM-DD>
**Rationale:** <the user's words, or "accepted the report recommendation" if they gave none>
```

The `**Chosen:** option-<x>` line must match `^\*\*Chosen:\*\* option-[abcd]$` exactly — Plan 05 Task 1 parses it to select which conversion branch to execute.

5. Commit with: `docs(phase-1): record lib/plank-monorepo tracking decision`

If the user selects `option-d`, first re-read the report's `### Local-only (present here, absent upstream)` section. If it lists any path, tell the user that option-d destroys that content and ask them to confirm again before recording it.
  </action>
  <decision>How should `lib/plank-monorepo` be tracked in this repository, given the measured drift from upstream?</decision>
  <context>
The locked decision in 01-CONTEXT.md was "all three lib/ dependencies become git submodules pinned to their upstream commits", qualified by "planner should still verify with a diff before deleting local copies; if a diff shows local modifications, stop and surface it".

Research (01-RESEARCH.md Pitfall 4) confirmed `lib/plank-monorepo` is NOT byte-identical to upstream HEAD nor to several sampled historical commits — 52 files differed across `plankc/frontend/**`, `plankc/sir/**` and `plank-tree-sitter/**`. The drift report from Task 1 has the current, exact numbers.

Whatever is chosen, `remappings.txt` line 4 (`solady/=lib/plank-monorepo/plankc/plank-diff-tests/lib/solady/src/`) must still resolve afterwards, and `lib/plank-monorepo` must stop being untracked — it is the last thing standing between this repo and INFRA-01.
  </context>
  <options>
    <option id="option-a">
      <name>Pin closest upstream commit + carry the local delta as a tracked patch</name>
      <pros>Keeps the upstream relationship explicit and updatable; the local delta becomes reviewable, named content rather than an anonymous 25M blob; smallest repo footprint; nested submodules resolve normally via recursive init</pros>
      <cons>The working tree no longer matches the submodule checkout until the patch is applied, so a fresh clone needs an extra apply step; brittle if upstream moves under the patch</cons>
    </option>
    <option id="option-b">
      <name>Keep vendored — track all files directly in this repo, no submodule</name>
      <pros>Simplest and most honest given confirmed drift; the working tree is exactly what is committed; no clone-time recursion needed</pros>
      <cons>Adds ~25M and ~1396 files to this repo's history; upstream updates become manual re-vendoring; diverges from the "all three become submodules" wording; REQUIRES two extra steps or it silently breaks — deleting the five dangling `.git` gitfiles, and force-adding the self-ignored `plankc/plank-diff-tests/lib/**` tree (otherwise only 825 of 1401 files commit and the `solady/` remapping dies on a fresh clone)</cons>
    </option>
    <option id="option-c">
      <name>Fork to a JMSBPP/plank-monorepo repo at the local state, submodule-pin the fork</name>
      <pros>Preserves both the submodule shape and the exact local content; upstream can be merged into the fork later; this repo stays small</pros>
      <cons>Requires creating and pushing a new GitHub repository (a network/account action, not purely local); adds a repo to maintain; contradicts the phase's "local-only, no CI" posture</cons>
    </option>
    <option id="option-d">
      <name>Pin upstream commit and discard the local delta</name>
      <pros>Cleanest possible submodule; exactly matches the original locked decision; zero maintenance</pros>
      <cons>Destroys whatever the 52-file delta contains — only acceptable if Task 1's Assessment concludes the drift is pure upstream progress with no local-only content</cons>
    </option>
  </options>
  <verify>
    <automated>grep -qE '^\*\*Chosen:\*\* option-[abcd]$' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md && test "$(grep -c '^## Decision$' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md)" -eq 1 && test "$(find lib/plank-monorepo -type f -not -path '*/.git/*' | wc -l)" -eq 1401 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Decision$' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md` outputs `1`
    - `grep -cE '^\*\*Chosen:\*\* option-[abcd]$' .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md` outputs `1`
    - The `## Decision` section also contains a `**Date:**` line and a `**Rationale:**` line
    - If `option-d` was recorded, the report's `### Local-only (present here, absent upstream)` section is empty or explicitly states `none`
    - `find lib/plank-monorepo -type f -not -path '*/.git/*' | wc -l` outputs `1401` (nothing converted or deleted in this plan)
    - `git log --oneline -1` subject is `docs(phase-1): record lib/plank-monorepo tracking decision`
  </acceptance_criteria>
  <done>A single `**Chosen:** option-[abcd]` line is committed in the drift report, with date and rationale; `lib/plank-monorepo` is untouched.</done>
  <resume-signal>Select: option-a, option-b, option-c, or option-d</resume-signal>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/01-project-hygiene-build-isolation/01-plank-monorepo-drift.md` exits 0
2. The report contains all eight required headings plus `## Decision`
3. Exactly one line matches `^\*\*Chosen:\*\* option-[abcd]$`
4. `find lib/plank-monorepo -type f -not -path '*/.git/*' | wc -l` outputs `1401` — nothing was converted or deleted in this plan
5. `./scripts/verify-repo-state.sh` still exits 1 with its single FAIL naming `lib/plank-monorepo/` (unchanged from Plan 03 — by design)
6. `git log --oneline -2` shows the drift measurement commit followed by the decision commit
</verification>

<success_criteria>
- The drift is a measured fact with commit-level evidence, not an assumption
- The two silent-breakage traps (dangling gitdir pointers, self-ignored `plank-diff-tests/lib/`) are quantified in the report
- The user has made an explicit, recorded choice among four options grounded in that evidence
- Nothing was converted or deleted — Plan 05 executes the decision
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-hygiene-build-isolation/01-04-SUMMARY.md`
</output>
