import std::constructor::return_runtime;
import types::Draft::*;


init { return_runtime();}

run {
    let selector = @evm_shr(224, @evm_calldataload(0));
    if selector == SELECTOR_SET_WAGE {
       	@evm_stop();
    }
    @evm_stop();
}